function isPalindrome(x: number): boolean {

// 1.convert number into string
// 2. reverse the string
//3. check if thr reverse string  same with the original string

let stringX = String(x);
stringX.split("").reverse().join() == stringX

     
};
