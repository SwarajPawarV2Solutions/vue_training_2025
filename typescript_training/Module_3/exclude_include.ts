let app: string = "Hello"

// To exclude this file we need to add the file name in tsconfig.json in exclude block.

// To include this file we need to add the file name in tsconfig.json in include block.


